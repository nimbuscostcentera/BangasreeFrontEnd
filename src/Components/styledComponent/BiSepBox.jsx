import Box from "@mui/material/Box";
import { styled } from "@mui/system";
const BiSepBox = styled(Box)({
  display: "flex",
  flexDirection:"row",
  flexWrap: "wrap",
  justifyContent: "space-between",
  alignItems: "center",
  color: "#000000",
});

export default BiSepBox;
